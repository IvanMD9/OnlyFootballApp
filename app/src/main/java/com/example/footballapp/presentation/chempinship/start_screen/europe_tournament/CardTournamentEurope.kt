package com.example.footballapp.presentation.chempinship.start_screen.europe_tournament

import com.example.footballapp.R

enum class CardTournamentEurope(
    val id: Int,
    val image: Int
) {
    CHAMPIONS(id = 0, image = R.drawable.champions),
}
